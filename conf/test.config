/*
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Nextflow config file for running minimal tests
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    Defines input files and everything required to run a fast and simple pipeline test.

    Use as follows:
        nextflow run MetaflowX -profile test,<docker/singularity> --outdir <OUTDIR>

----------------------------------------------------------------------------------------
*/

docker.enabled = true

params {
    config_profile_name        = 'Test profile'
    config_profile_description = 'Minimal test dataset to check pipeline function：test'

    // Limit resources so that this can run on GitHub Actions
    max_cpus   = 8
    max_memory = '16.GB'
    max_time   = '24.h'
    
    input = "${baseDir}/test/samplesheet.csv"
    function = "test"
    
}

process {
    withName: FASTPONLY {
        publishDir = [
            path: {"${params.outdir}/01.CleanData/${meta.id}"},
            pattern: "*.gz",
            mode: params.publish_dir_mode,
            failOnError: true   
        ]
    }    

}